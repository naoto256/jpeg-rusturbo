//! x86_64 SIMD kernels — placeholder.
//!
//! Reserved for the upcoming AVX2 port (translation of libjpeg-turbo's
//! `simd/x86_64/*.asm`). Currently empty and not used as the dispatch
//! backend; on x86_64 we fall back to `arch::scalar`.
